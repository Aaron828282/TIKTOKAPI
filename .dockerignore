# 构建上下文里不需要的东西
.git
.gitignore
node_modules
.env
.env.*
session*.json
*local*.json
# ⚠️ 不能整目录排除 certs —— 只留号池自签证书（公钥，非机密），
#    Dockerfile 要 COPY 它做身份钉死（RH_POOL_CA_FILE）。
certs/*
!certs/pool-ca.crt
reference
__pycache__
*.py
*.pyc
*.md
!README.md

# 自测夹具：里面是假的 TikTok 上游，镜像里用不到（那是给本地/CI 跑的）
selftest
