# 凭据与本地产物一律不进版本库。
#
# ⚠️ 这个仓库是 **Public** —— 一旦误提交 cookie / agent_token，
#    等于把号池的派单权和 TikTok 登录态一起公开。
#    下面的规则是兜底，不是许可：提交前仍然要 `git status` 扫一眼。

node_modules/

# 会话凭据（TikTok 登录态）
.env
.env.*
!.env.example
session.json
session*.json
*credentials*.json
*local*.json

# 「Copy as cURL」的原始文本 —— 里面是**完整 cookie**，等同账号控制权
curl*.txt
*.curl
curl.txt

# 私钥 / 证书
#
# ⚠️ 必须写 `certs/*` 而不是 `certs/`。
#    Git 不允许「重新包含一个位于被排除目录下的文件」——
#    写成目录形式后，下面的 `!certs/pool-ca.crt` 会被**静默忽略**，
#    文件一直不进版本库，`git status` 里也看不出来（只显示 `!! certs/`）。
#    `.dockerignore` 里的写法同理（用的是同一套忽略语义）。
certs/*
!certs/pool-ca.crt
*.pem
*.key
!reference/**

# 运行产物
data/
*.log
logs/

# Python 参考实现的缓存（reference/sigv4.py 本身要保留，它是移植的依据）
__pycache__/
*.pyc
